import infinity from './spin.gif';
const Spinner = () => {
    return (
            <img alt ="" src={infinity} style={{ margin: '0 auto', width:'auto', height:'250px',  display:'block'}} />
        )
}

export default Spinner;